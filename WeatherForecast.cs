using System;

namespace CryptowatcherA
{
    public class WeatherForecast
    {
        public DateTime Date { get; set; }

        public int TemperatureC { get; set; }

        public int TemperatureF => 32 + (int)(TemperatureC / 0.5556);

        public string Summary { get; set; }


//         ng update @angular/core@8 @angular/cli@8

// ng update @angular/core@9 @angular/cli@9

// npm outdated

// Npm update

// ng add @angular/localize

// -----------------------------------

// ng update --force @angular/core@10 @angular/cli@10

// npm install --save @ng-select/ng-select@5.0.0

// npm remove @nguniversal/module-map-ngfactory-loader

// Remove codelyser
// Remove ng-toggle-button
// ng update @ng-bootstrap/ng-bootstrap
// npm install ng-toggle-button
// Npm install codelyser
// ng update @angular/core @angular/cli
    }
}
